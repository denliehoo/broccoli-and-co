import { EInviteModalContent } from './enum';

export const INVITE_MODAL_TITLE: Record<EInviteModalContent, string> = {
  [EInviteModalContent.FORM]: 'Request an invite',
  [EInviteModalContent.SUCCESS]: 'All done!',
};
